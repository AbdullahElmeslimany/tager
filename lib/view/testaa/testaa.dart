import 'package:flutter/material.dart';

class TestAppaa extends StatelessWidget {
  const TestAppaa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: MaterialButton(
          onPressed: () {},
          child: Text("data"),
        ),
      ),
    );
  }
}
